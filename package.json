{
  "name": "tweetobot-docs",
  "version": "2.0.1",
  "private": true,
  "scripts": {
    "dev": "sphinx-build -b html source build",
    "serve": "sphinx-build -b html source build && cd build && python -m http.server 80",
    "done": "git add . && git cz"
  },
  "dependencies": {
  },
  "devDependencies": {
    "cz-conventional-changelog": "^3.3.0"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
